extend type Query {
    driver(id: ID!): Driver
    drivers: [Driver]
}

extend type Mutation {
    createDriver(
        licenseNumber: String!
        userId: String!
        vehicleId: ID!): Driver
}

type Driver {
    id: ID!,
    userId: String!
    licenseNumber: String!
    vehicleId: ID!
}



